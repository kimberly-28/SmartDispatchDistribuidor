import { FilterSitesPipe } from './filter-sites.pipe';

describe('FilterSitesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSitesPipe();
    expect(pipe).toBeTruthy();
  });
});
